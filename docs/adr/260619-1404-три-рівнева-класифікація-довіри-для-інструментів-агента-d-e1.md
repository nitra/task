---
session: e8e91f68-dadd-4c55-b6c2-b011d44e5a82
captured: 2026-06-19T14:04:31+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-nitra-task/e8e91f68-dadd-4c55-b6c2-b011d44e5a82.jsonl
---

Аналізую транскрипт і формую ADR-блоки.

```markdown
## ADR Три-рівнева класифікація довіри для інструментів агента (D-E1)

## Context and Problem Statement
Gateway-агент (зовнішній LLM або людина) може викликати будь-який інструмент із каталогу. Потрібно розмежувати, що агент може виконати самостійно, а що вимагає присутності людини, щоб не допустити неконтрольованих деструктивних дій з боку автономного LLM.

## Considered Options
* Бінарна схема allow/deny для агента
* Три-рівнева класифікація `allow | approval | deny` через `classify(actor, tool)`
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Три-рівнева класифікація `allow | approval | deny` через `classify(actor, tool)`", because вона дає агенту видимість деструктивних інструментів (щоб він міг їх *запросити*), водночас блокуючи автономне виконання — тобто `deny` не хований від агента, а `approval` є проміжним станом між allow і deny.

### Consequences
* Good, because агент може природною мовою запросити деструктивну дію, а логіка примусового підтвердження людиною реалізована на рівні `dispatch`-gate, а не лише на рівні маніфесту.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
Реалізовано в `app/src/tool/scope.js` (`classify`, `scopedManifest`); gate вбудований у `runAgent` (`app/src/tool/llm.js`). Тул-каталог (`app/src/tool/catalog.js`) розширено полем `tier: 'read' | 'write' | 'destructive'`. Тести: `app/src/tests/scope.test.js`. Коміт `24147e7`.
```

```markdown
## ADR Деструктивне видалення задачі через human-approval з retryable-семантикою (D-E2)

## Context and Problem Statement
Після реалізації D-E1 агент не міг видалити задачу (тул `delete` був відсутній). Виникла потреба дати агенту можливість *запросити* видалення, водночас зберігши виконання виключно за людиною. Додатково: перша спроба approve у старій збірці (без `delete_task` у Rust) завершилась помилкою і статус записав `failed` — повторити approve було неможливо.

## Considered Options
* Дозволити агенту виконувати `delete` автономно
* Реалізувати тул `delete` (tier `destructive`) із паузою `needs_approval` та людським підтвердженням в аудит-UI
* Залишити `delete` відсутнім
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Реалізувати тул `delete` із паузою `needs_approval` та людським підтвердженням", because деструктив через людину — принципова вимога дизайну; агент *запитує*, людина *виконує*. Approve retryable: якщо виконання `delete_task` падає з помилкою (наприклад, Rust-команда відсутня в старій збірці), статус лишається `needs_approval` (а не `failed`) — щоб людина могла повторити підтвердження без нового запиту від агента.

### Consequences
* Good, because transcript фіксує очікувану користь: live-демо показало `status: needs_approval`, задача `mcp-demo` лишилась недоторканою; після рестарту з новою Rust-збіркою approve спрацював і задачу видалено.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
Rust-команда `delete_task` — `app/src-tauri/src/lib.rs`; валідує ім'я та перевіряє, що шлях лишається під `tasks_dir`, тоді `remove_dir_all`. Approve/reject UI — `app/src/components/AuditDialog.vue` (кнопки «Підтвердити / Відхилити» для записів зі статусом `needs_approval`). `handleApprove` — `app/src/tool/agent-handler.js`. Retryable-фікс: коміт `aa45100`. D-E2 загалом: коміт `690fe1a`. Тести `app/src/tests/agent-handler.test.js` (51 JS).
```

```markdown
## ADR Ін'єкція списку воркспейсів у system-prompt для розмовного ґраундингу

## Context and Problem Statement
Агент отримує запити у вільній формі («які задачі відкриті в abie k8s»), але для виклику тулу `scan` йому потрібен абсолютний `tasksDir`. Виникла потреба дати агенту можливість зіставити розмовну назву з конкретним шляхом без додаткових кроків.

## Considered Options
* Агент сам викликає тул `workspaces` і шукає відповідність (multi-step)
* Вкласти список `{ label → path }` у system-prompt перед запуском агента

## Decision Outcome
Chosen option: "Вкласти список `{ label → path }` у system-prompt", because локальна 4B-модель (Gemma) ненадійна в multi-step + fuzzy-match; коли маппінг «назва → шлях» вже у промпті, агенту лишається один крок — `scan` за готовим шляхом. Функція `createSystemPrompt(workspaces)` оновлена, `handleRequest` тягне `workspaces` перед запуском loop і вкладає в промпт.

### Consequences
* Good, because transcript фіксує очікувану користь: live-демо — запит «Which tasks are open in nitra task?» без шляху → агент зіставив «nitra task» з `nitra/task` і повернув список відкритих задач одним кроком.
* Bad, because якщо конфігурований список проєктів порожній або проєкт не містить `mt/`, він не зʼявиться у підказці й агент не зможе зіставити назву.

## More Information
`createSystemPrompt` — `app/src/tool/llm.js`; `listWorkspaces` (guard + try/catch) + виклик на початку `handleRequest` — `app/src/tool/agent-handler.js`. Коміт `245f118`. Ґраундинг використовує лейбли відносно кореня проєктів (напр. `nitra/task`, `abie/k8s`), що з'являються завдяки рішенню ADR про точку відліку.
```

```markdown
## ADR Єдине джерело project paths у Rust config-файлі з підтримкою кількох коренів

## Context and Problem Statement
GUI зберігав список project paths у localStorage (недоступний для Rust і node-біна), а агент ґраундив від хардкод-значення `~/www`. При додаванні нового кореня (`/Users/vitalii/www/abie`) людина бачила його у списку, тоді як агент продовжував ґраундити від `~/www` — точки відліку розходились.

## Considered Options
* Дзеркалювання (localStorage + запис у `config.json` при кожній зміні)
* `appLocalDataDir/config.json` як єдине джерело (`config-as-source`): GUI, Rust-агент, MCP-бін читають той самий файл

## Decision Outcome
Chosen option: "`config-as-source` — `appLocalDataDir/config.json`", because це єдине джерело правди, доступне всім трьом шарам (Rust/вебвʼю/node), відповідає вимозі «вся FS → Rust» і усуває ризик дрейфу між двома сховищами. Дефолт при порожньому/відсутньому файлі — `[~/www]`, щоб збірка працювала з коробки. Підтримка кількох коренів (add/remove) — вже існувала в UI, тепер передається в Rust і `mt-scanner`.

### Consequences
* Good, because transcript фіксує очікувану користь: live-демо показало, що бін `workspaces` читає конфіг і повертає `nitra/cursor`, `nitra/task`; GUI та агент бачать одні й ті ж воркспейси.
* Bad, because `use-project-paths` стає async-load (composable тепер чекає Rust-виклику); `list_projects_from_paths` лишилася як мертва команда і потребує видалення через knip.

## More Information
Rust-модуль `app/src-tauri/src/config.rs` (`read_config_in`, `write_config_in`, `default_project_paths` → `[~/www]`, Tauri-команди `get_project_paths` / `set_project_paths`). `find_all_tasks_dirs` переписана на `list_projects_from_paths(get_project_paths())`. `use-project-paths.js` — `app/src/composables/use-project-paths.js` (localStorage прибрано, invoke). MCP-бін `app/bin/task.mjs` — читає `config.json` і передає шляхи у `mt-scanner workspaces <dir...>`. `mt-scanner` side — `scanner/src/main.rs` (multi-dir). Коміти: task `81d3c60`, mt `7d73b95`. Rust config-тести: 3/3.
```
