---
session: b4afe071-8235-4897-8904-2816364ba716
captured: 2026-06-07T21:11:58+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-task/b4afe071-8235-4897-8904-2816364ba716.jsonl
---

Symlink створено. Він буде правильно зберігатись у git як відносний шлях. На будь-якій машині де `cursor` і `task` склоновані в одну папку `nitra/` — все працюватиме.

Єдина умова: при клонуванні на новому комп'ютері потрібно склонувати **обидва** репо в одну батьківську папку (наприклад `~/www/nitra/`), інакше symlink буде битим.

---

Now I'll produce the ADR documentation from this session transcript.

## ADR Додавання деталей задачі через клік на рядок

## Context and Problem Statement
У застосунку `nitra/task` (Tauri + Vue) відображався лише список задач без можливості переглянути вміст окремої задачі. Користувач хотів клікнути на рядок задачі та побачити її деталі.

## Considered Options
* Відкривати деталі у `q-dialog` (модальне вікно)
* Відкривати деталі у `q-drawer` (бічна панель)
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "`q-dialog` з фіксованою шириною", because початково використовувався `maximized`-режим, але його скасовано через те що він займав весь екран — обрано звичайний діалог з `width: 600px` та `fade`-анімацією.

### Consequences
* Good, because transcript фіксує очікувану користь: клік на рядок задачі в Tauri-вікні відкриває діалог з відрендереним markdown-вмістом `task.md`.
* Bad, because `invoke` з `@tauri-apps/api/core` не працює у звичайному браузері — тестування можливе лише у рідному Tauri webview.

## More Information
- Змінені файли: `app/src/components/TaskNodeItem.vue`, `app/src/components/TaskGraph.vue`, `app/src-tauri/src/lib.rs`
- Додана Rust-команда `read_task(tasks_dir, task_path)` — читає `task.md` за шляхом `{tasks_dir}/{task_path}/task.md`
- Команда зареєстрована в `tauri::generate_handler![scan_tasks, find_tasks_dir, read_task]`
- Встановлено залежність `marked@18.0.5` для рендерингу markdown у frontend
- `TaskNodeItem.vue` емітує подію `select` з об'єктом `node` при кліку на рядок
- `TaskGraph.vue` перехоплює подію, викликає `invoke('read_task', ...)`, рендерить результат через `marked.parse()`

---

## ADR Відносний symlink між репо `cursor` і `task`

## Context and Problem Statement
Файл `cursor/docs/думка.MD` з дизайном деталей задач потрібен в двох місцях (`cursor` і `task`), але зберігатись може тільки в одному. Абсолютний symlink зламається при клонуванні на іншому комп'ютері.

## Considered Options
* Відносний symlink (відносний шлях між двома репо-сусідами)
* Абсолютний symlink
* Окреме shared-репо
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "відносний symlink `../../cursor/docs/думка.MD`", because обидва репо (`cursor` і `task`) завжди є сусідами під `nitra/`, тому відносний шлях стабільний на будь-якій машині де дотримується ця структура.

### Consequences
* Good, because git зберігає symlink як текстовий рядок з відносним шляхом — він правильно клонується і відпрацьовує на новому комп'ютері якщо обидва репо в одній батьківській папці.
* Bad, because при клонуванні на новому комп'ютері обидва репо (`cursor` і `task`) мають бути склоновані в одну батьківську директорію — інакше symlink буде битим.

## More Information
- Symlink: `task/docs/думка.MD -> ../../cursor/docs/думка.MD`
- Команда: `ln -s ../../cursor/docs/думка.MD /Users/vitaliytv/www/nitra/task/docs/думка.MD`
- Вимога: обидва репо клонувати в спільну батьківську папку (наприклад `~/www/nitra/`)
