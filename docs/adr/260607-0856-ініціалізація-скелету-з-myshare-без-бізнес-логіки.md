---
session: 43607719-391b-42fd-8353-905e3494d931
captured: 2026-06-07T08:56:50+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-task/43607719-391b-42fd-8353-905e3494d931.jsonl
---

## ADR Ініціалізація скелету з myshare без бізнес-логіки

## Context and Problem Statement
Новий репозиторій `/Users/vitaliytv/www/nitra/task` був порожнім (лише `.git`). Потрібно було швидко отримати робочий Tauri + Vue 3 + Quasar + Bun monorepo scaffold для нового застосунку без повторення бізнес-модулів з myshare.

## Considered Options
* Скопіювати структуру з існуючого проєкту myshare, видаливши бізнес-логіку
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Скопіювати структуру з myshare, видаливши бізнес-логіку", because користувач явно вказав: «перенеси з нього структуру без бізнес логіки, каркас у них буде схожий».

### Consequences
* Good, because transcript фіксує очікувану користь: Vite dev server стартує за 2908 ms, Playwright підтверджує Quasar layout рендериться коректно на `http://localhost:1420`.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
Перенесено: `package.json`, `bunfig.toml`, `.gitignore`, `eslint.config.js`, `knip.json`, `app/package.json`, `app/index.html`, `app/vite.config.js`, `app/jsconfig.json`, `app/vitest.config.js`, `app/src/main.js`, `app/src/App.vue`, `app/src/quasar-variables.sass`, `app/src-tauri/tauri.conf.json`, `app/src-tauri/Cargo.toml` (без youtube/reqwest/tokio), `app/src-tauri/src/lib.rs` (порожній invoke_handler), `app/src-tauri/capabilities/default.json`. Не перенесено: `scripts/` workspace, stryker configs, модулі `youtube`, `ollama`, `shared-url`, `page-meta`, `caption-langs`, `url-history`. Tauri identifier: `com.nitra.task`.

---

## ADR Symlink-workaround для відсутнього `lib/` у `@nitra/cursor`

## Context and Problem Statement
`npx @nitra/cursor fix` падав з `Cannot find module '…/node_modules/@nitra/cursor/lib/models.mjs'`. Директорія `lib/` відсутня в опублікованому tarball як `@nitra/cursor@3.29.0`, так і `@nitra/cursor@4.0.0` — поле `files` у `package.json` пакету не включало `"lib"`.

## Considered Options
* Symlink з локального source-репо: `node_modules/@nitra/cursor/lib → /Users/vitaliytv/www/nitra/cursor/npm/lib`
* Оновити до `^4.0.0` (але v4.0.0 теж опублікована без `lib/`)
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Symlink з локального source-репо", because `@nitra/cursor@4.0.0` теж опублікована без `lib/` — оновлення версії не вирішує проблему; symlink із `/Users/vitaliytv/www/nitra/cursor/npm/lib` дозволив `npx @nitra/cursor fix` запуститись негайно.

### Consequences
* Good, because transcript фіксує очікувану користь: `npx @nitra/cursor fix` запустився і почав виправляти порушення.
* Bad, because symlink у `node_modules` не survives `bun install --clean` — workaround треба відтворювати після чистої інсталяції. Справжній фікс — перепублікувати `@nitra/cursor` з `"lib"` у полі `files`.

## More Information
Команда: `ln -s /Users/vitaliytv/www/nitra/cursor/npm/lib node_modules/@nitra/cursor/lib`. Файл, що бракував: `lib/models.mjs` — імпортується з `skills/fix/js/llm-worker.mjs:7` та `scripts/coverage-fix.mjs`. Корінь проблеми: поле `files` у `npm/package.json` не містило `"lib"` у v3.29.0 і v4.0.0.

---

## ADR Конфігурація `.oxlintrc.json` за зразком myshare (порожні categories)

## Context and Problem Statement
`npx @nitra/cursor fix` генерував `.oxlintrc.json` з усіма категоріями `"deny"` (canonical config з пакету). Це спричинило каскадні oxlint-помилки: `no-default-export` у `vite.config.js`/`vitest.config.js`, `unicorn/filename-case` у `App.vue`, `import/unambiguous` у `vite-env.d.ts` — файлах, де ці правила не застосовні.

## Considered Options
* Використати canonical-конфігурацію `@nitra/cursor` (всі categories: `"deny"`)
* Використати конфігурацію за зразком myshare (`categories: {}`, вибіркові `rules`)

## Decision Outcome
Chosen option: "Конфігурація за зразком myshare", because myshare — перевірений робочий проєкт з тим самим стеком (Tauri + Vue 3 + Quasar); canonical config із усіма `"deny"` ламав vite/vitest default exports і Vue SFC tooling.

### Consequences
* Good, because після заміни `bunx oxlint .` повернув нульовий вихід (за винятком двох `catch-error-name` у `TaskGraph.vue`, виправлених окремо).
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
Файл: `.oxlintrc.json`. Ключова різниця: `"categories": {}` замість `"correctness":"deny","suspicious":"deny","pedantic":"warn"`. Додані `ignorePatterns`: `"app/src-tauri/target/**"`, `"app/src-tauri/gen/**"`, `"app/dist/**"`. Плагін `"vue"` доданий до `plugins` (був відсутній у canonical). Виправлення `TaskGraph.vue`: `catch (e)` → `catch (error_)` (правило `unicorn/catch-error-name`).
