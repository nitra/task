---
session: 005a85c1-1524-4ddb-aece-77409f4a82d1
captured: 2026-06-15T05:32:29+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-nitra-task/005a85c1-1524-4ddb-aece-77409f4a82d1.jsonl
---

The fix is in `capabilities/default.json` — I added port-specific patterns for common local LLM ports so Tauri's HTTP scope now covers them:

```json
{ "url": "http://127.0.0.1:*" },
{ "url": "http://localhost:*" },
```

These port-wildcard patterns (`*`) match any port on loopback addresses. The existing `http://**` stays for non-loopback HTTP, but Tauri's URL pattern engine requires an explicit port glob when a non-default port is used with a literal hostname.

---

[assistant]
## ADR Tauri HTTP-плагін: дозвіл локальних портів

## Context and Problem Statement
Фронтенд (`AgentDialog.vue`) використовує `@tauri-apps/plugin-http` для запитів до локального LLM-ендпоінта (`http://127.0.0.1:8000/...`). Tauri відхиляв запит через те, що наявний шаблон `http://**` у scope не покривав URL із явно вказаним нестандартним портом.

## Considered Options
* Додати явні port-wildcard шаблони для loopback-адрес (`http://127.0.0.1:*`, `http://localhost:*`)
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Додати явні port-wildcard шаблони для loopback-адрес", because Tauri's URL pattern engine вимагає явного `:*` для нестандартних портів при literal hostname; `http://**` покриває лише default-порт (80) для hostname-wildcard.

### Consequences
* Good, because transcript фіксує очікувану користь: запити до локального LLM (та будь-яких інших loopback-сервісів на довільному порту) тепер проходять крізь Tauri HTTP scope.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Файл: `app/src-tauri/capabilities/default.json`
- Шаблони додано: `{ "url": "http://127.0.0.1:*" }`, `{ "url": "http://localhost:*" }`
- Існуючий шаблон `http://**` збережено для non-loopback HTTP.
- Компонент, що виконує запит: `app/src/components/AgentDialog.vue` (імпортує `fetch` з `@tauri-apps/plugin-http`).
