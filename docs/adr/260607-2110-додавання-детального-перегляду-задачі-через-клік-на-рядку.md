---
session: b4afe071-8235-4897-8904-2816364ba716
captured: 2026-06-07T21:10:08+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-task/b4afe071-8235-4897-8904-2816364ba716.jsonl
---

До речі, `nitra/` — це великий монорепо? Якщо так, то і `task`, і `cursor` можуть бути воркспейсами в ньому. Тоді `думка.MD` просто живе в `cursor/docs/` і шлях завжди відносний від кореня `nitra/`.

---

## ADR Додавання детального перегляду задачі через клік на рядку

## Context and Problem Statement
У Tauri-застосунку `nitra/task` відображався список задач, але не було можливості переглянути вміст `task.md` конкретної задачі. Користувач запитав, де клікнути щоб побачити деталі, і виявилось що такого функціоналу не існувало.

## Considered Options
* Додати клік на рядок задачі з відображенням `task.md` у діалозі (modal dialog)
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Діалог з markdown-рендерингом вмісту task.md", because користувач підтвердив «давай» після пропозиції реалізувати відкриття drawer/діалогу з вмістом `task.md`.

### Consequences
* Good, because transcript фіксує очікувану користь: користувач підтвердив «там працює» після реалізації — функціонал працює у рідному Tauri-вікні.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Новий Tauri-command `read_task(tasks_dir: String, task_path: String) -> Result<String, String>` у `app/src-tauri/src/lib.rs`, зареєстрований у `invoke_handler`.
- Встановлена залежність `marked@18.0.5` (`app/package.json`) для рендерингу markdown.
- Оновлені компоненти: `app/src/components/TaskNodeItem.vue` (emit `select` при кліку на рядок), `app/src/components/TaskGraph.vue` (обробник `select`, `q-dialog` з рендереним HTML).
- `invoke` працює лише у Tauri webview; у звичайному браузері (Chrome) IPC-міст відсутній.

---

## ADR Symlink vs. альтернативи для спільного файлу між двома репозиторіями

## Context and Problem Statement
Файл `/Users/vitaliytv/www/nitra/cursor/docs/думка.MD` потрібен в обох репозиторіях (`cursor` і `task`), але зберігатися може лише в одному місці. Репозиторії клонуються на різних машинах, тому рішення має бути портативним.

## Considered Options
* Відносний symlink (git зберігає як текстовий файл з відносним шляхом)
* Git submodule (`cursor/docs` як субмодуль у `task`)
* Файл лише в одному репо, у другому посилання через шлях у документації або CLAUDE.md

## Decision Outcome
Chosen option: рішення залишається відкритим — transcript не зафіксував остаточного вибору; обговорення ще тривало наприкінці сесії.

### Consequences
* Good, because відносний symlink портативний між машинами якщо структура `nitra/cursor` + `nitra/task` зберігається.
* Bad, because відносний symlink ламається якщо репозиторії клоновано в різні директорії відносно одне одного.

## More Information
- `task` і `cursor` — окремі git-репозиторії з коренями `/Users/vitaliytv/www/nitra/task` і `/Users/vitaliytv/www/nitra/cursor`.
- Обидва розміщені в монорепо-директорії `/Users/vitaliytv/www/nitra/` поруч з іншими воркспейсами.
- Відносний шлях symlink: `../../cursor/docs/думка.MD` від `task/docs/думка.MD`.
