---
session: 523a2d0e-a2db-4ca8-ad75-b13d453965a4
captured: 2026-06-08T17:46:53+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-task/523a2d0e-a2db-4ca8-ad75-b13d453965a4.jsonl
---

---

## ADR Структура задач у форматі MT (`mt/`)

## Context and Problem Statement

Проект зберігав задачі у директорії `tasks/` у форматі без специфікації версії і без встановленого зв'язку з пакетом `@7n/mt`. Специфікація `@7n/mt` вимагає розташування вузлів у `mt/`, певного набору полів у frontmatter, sentinel-файлів `a.md`/`h.md` для статусу виконавця та директорії `deps/` для залежностей.

## Considered Options

* Мігрувати існуючі `tasks/` → `mt/` відповідно до специфікації `@7n/mt`
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome

Chosen option: "Мігрувати існуючі `tasks/` → `mt/`", because користувач прямо вказав перенести задачі до директорії `mt/` та привести їх у відповідність до стандарту `@7n/mt`.

### Consequences

* Good, because transcript фіксує очікувану користь: задачі тепер зберігаються у `mt/open-in-editor/task.md` та `mt/open-in-editor-local/task.md` з полем `schema_version: 1`; залежність між задачами виражена через `mt/open-in-editor-local/deps/open-in-editor.md`.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information

- Специфікація: `node_modules/@7n/mt/docs/mt.md`
- Документ-посилання: `docs/mt.md`
- Конфіг: `.mt.json` зі значеннями `mt_dir: "./mt"`, `worktrees_dir: "./.worktrees"`, `agent_concurrency: 5`, `default_budget_sec: 3600`
- Стара директорія `tasks/` видалена через `rm -rf /Users/vitaliytv/www/nitra/task/tasks`

---

## ADR Детектування стану задачі: файли-sentinel замість frontmatter

## Context and Problem Statement

Tauri-додаток (бекенд `lib.rs`) визначав стан задачі з поля `mode` у YAML-frontmatter `task.md` і шукав файли `outputs_*.md` як маркер `Resolved`. Специфікація `@7n/mt` використовує окремі файли у директорії вузла (`a.md`/`h.md`, `fact_*.md`, `running_<pid>_until_*`) і директорію `deps/` замість YAML-поля.

## Considered Options

* Оновити `lib.rs` відповідно до файло-орієнтованого механізму `@7n/mt`
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome

Chosen option: "Оновити `lib.rs` відповідно до файло-орієнтованого механізму `@7n/mt`", because користувач доручив "пропатчити таурі-додаток", маючи на увазі приведення у відповідність до нового формату MT.

### Consequences

* Good, because transcript фіксує очікувану користь: `cargo check` і `cargo clippy` проходять без помилок; стан `Unassigned` додано для вузлів без `a.md`/`h.md`.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information

- Змінений файл: `app/src-tauri/src/lib.rs`
- `fact_*.md` замість `outputs_*.md` → стан `Resolved`
- `a.md` → режим `agent`, `h.md` → режим `human`, відсутність обох → `Unassigned`
- `deps/` директорія замість YAML-поля `deps`
- Новий стан `TaskState::Unassigned` доданий і відображений у `TaskGraph.vue` та `TaskNodeItem.vue`
- `find_tasks_dir` оновлено: `.mt.json` → поле `mt_dir`, потім legacy `.n-cursor.json`, потім `mt/` і `tasks/` як fallback

---

## ADR Групування задач по workspace у Tauri UI

## Context and Problem Statement

Tauri-UI відображав задачі одного `mt/`-директорія через ручне текстове поле. Монорепо може мати кілька `mt/`-директорій у різних пакетах, які виникають динамічно. Користувач вимагає: відображати задачі з усіх workspace одночасно, групуючи їх за workspace, без ручного вводу шляху.

## Considered Options

* Динамічне сканування від git root + відображення всіх workspace згрупованими (без input-поля)
* Статичний список workspace збережений у `localStorage`
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome

Chosen option: "Динамічне сканування від git root + відображення всіх workspace згрупованими", because `localStorage` не підходить через динамічну появу нових workspace; завжди потрібен актуальний скан.

### Consequences

* Good, because transcript фіксує очікувану користь: `find_all_tasks_dirs` повертає список усіх `mt/`-директорій від git root; UI показує кожен workspace окремою секцією.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information

- Новий Rust-command: `find_all_tasks_dirs() -> Vec<WorkspaceInfo>`
- `WorkspaceInfo { label: String, path: String }` — label є відносним шляхом від git root
- `find_git_root` шукає `.git` директорію вгору по дереву
- `scan_for_workspaces` — рекурсивний обхід з глибиною до 6 рівнів, пропускає `node_modules`, `target`, `dist`, `build`, приховані директорії
- `TaskGraph.vue` переписано: немає `q-input`, виводяться секції per-workspace

---

## ADR Виключення gitignore-директорій зі сканування workspace

## Context and Problem Statement

`scan_for_workspaces` рекурсивно обходить git-репозиторій у пошуку `mt/`-директорій, але не враховував `.gitignore`. `mt/` не може знаходитись у `.gitignore`d-директорії (файли не відстежуватимуться git), тому такі директорії треба виключати зі сканування.

## Considered Options

* Читати `.gitignore` на кожному рівні рекурсії і пропускати заігноровані піддиректорії
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome

Chosen option: "Читати `.gitignore` на кожному рівні рекурсії і пропускати заігноровані піддиректорії", because користувач прямо вказав це як вимогу і зазначив, що документацію треба оновити відповідно.

### Consequences

* Good, because transcript фіксує очікувану користь: заігноровані директорії (наприклад `dist/`, `*.cache`) не сканується, `mt/` не може фізично існувати в `.gitignore`d-директорії.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information

- Документація оновлена: `/Users/vitaliytv/www/nitra/mt/npm/docs/mt.md`, секція "Монорепо"
- `lib.rs`: нові функції `load_gitignore(dir)`, `is_gitignored(name, patterns)`, `glob_match_name(name, pattern)` — обробляє прості `*`-glob-паттерни
- `scan_for_workspaces` тепер приймає `parent_ignores: &[String]`, об'єднує їх з локальним `.gitignore` і передає нащадкам
