---
session: 5a27ef2b-566f-45fb-9a11-25dbbc20b542
captured: 2026-06-19T13:18:46+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-nitra-task/5a27ef2b-566f-45fb-9a11-25dbbc20b542.jsonl
---

Усі три звіти отримано. Синтезую.

## Кандидати на перенесення в `@7n/tauri-components`

### 🔴 Сильно рекомендовано (дублюється у всіх трьох, нульова доменна прив'язка)

**`test-utils/quasar.js` → пакет як `@7n/tauri-components/testing`**

Усі три додатки мають майже ідентичний файл `mountQuasar(component, options)` — монтування Vue-компонента з Quasar-плагінами для vitest. Різниця лише в назві функції (`mountQ` vs `mountQuasar`). Це чистий boilerplate з нульовим доменним контентом.

---

### 🟡 Є сенс, але треба твоє рішення

**`page-meta.js` (myshare) → пакет**

Отримує title/favicon для URL через Tauri http-plugin. У task немає, але будь-який Tauri-додаток, який показує посилання, захоче це. Невеликий (~40 рядків), без стану, чистий IO.

**`platform.js` (myshare) → пакет**

`isAndroidPlatform()` — один рядок (`navigator.userAgent.includes('Android')`). Дуже маленький, але task і mlmail теж розрізняють desktop/mobile (через Tauri `isMobile`/`os()`). Якщо уніфікувати — зменшить розбіжність.

---

### 🟢 Залишити де є (domain або не варте витрат)

- **OAuth flow** (mlmail) — Gmail-специфічний, нікуди не йде
- **`url-history.js`** — вже видалено при міграції на OPFS
- **`shared-url.js`** (myshare) — Android share intent, специфічний
- **`omlx.js`** (myshare translate) — вже оркеструється через agent-kit пакета

---

## Моя рекомендація

**Почати з `test-utils` → `@7n/tauri-components/testing`** — найбільша вигода, нульовий ризик, і всі три додатки одразу позбудуться boilerplate. Після — опціонально `page-meta.js`.

Хочеш, щоб я виніс `mountQuasar` у пакет і замінив у всіх трьох?
