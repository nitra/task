---
session: e8e91f68-dadd-4c55-b6c2-b011d44e5a82
captured: 2026-06-14T11:31:19+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-nitra-task/e8e91f68-dadd-4c55-b6c2-b011d44e5a82.jsonl
---

Готово — всі 4 рішення зафіксовані в **`docs/specs/260614-n-tool-surface.md`** і стара чернетка видалена.

## Підсумок рішень

| # | Рішення | Вибір |
|---|---|---|
| 1 | Термін/правило/тека/CLI | `tool` → `n-tool-surface` → `src/tool/` → машинний CLI лише |
| 2 | LLM-провайдер / протокол | MLX+Gemma 3n E4B через **omlx** (OpenAI-сумісний); tools-маніфест OpenAI-формат; MCP — ціль пізніше |
| 3 | Headless-точка входу (MVP) | per-verb spawn → наявні `mt-scanner scan/workspaces/create` (mt-репо не чіпаємо) |
| 4 | Rust↔JS канон | навмисне розходження per-stack; ядро правила платформо-незалежне, деталі Tauri/Capacitor/Vue — в профільних правилах |

## Структура правила

Ядро (у `n-tool-surface.mdc`): що таке тул-поверхня, контракт `dispatch`, інваріант паритету, формат маніфесту.
Секція «Tauri»: handler = Rust-крейт, JS-каталог = схема+делегація через `invoke`, оркестратор спавнить бінарник.
Секція «Web (без Rust)»: handler = JS-callable, оркестратор через Node/Bun.

## MVP-план (реалізація)

1. `src/tool/index.js` — каталог (scan, workspaces, create) + `dispatch`
2. `src/tool/manifest.js` → OpenAI tools-маніфест
3. `bin/task.mjs` → машинний CLI: `task <tool> '<json>'` + `task list` + `task schema`
4. Рефактор `TaskGraph.vue` + `CreateTaskDialog.vue` → через `dispatch` (не прямий `invoke`)
5. `src/tool/llm.js` → HTTP до omlx + tool-loop
6. Тести: паритет (task list = dispatch names = manifest names), dispatch smoke-тести

Стартуємо реалізацію?
[user]
зроби summary сесії у вигляді ADR

---
