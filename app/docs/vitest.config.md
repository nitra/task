---
type: JS Module
title: vitest.config.js
resource: app/vitest.config.js
docgen:
  crc: 094ab03c
  model: omlx/gemma-4-e4b-it-OptiQ-4bit
  tier: local-min
  score: 100
  issues: judge:inaccurate:0.99
  judgeModel: openai-codex/gpt-5.4-mini
---

## Огляд

Я розумію. Як технічний письменник, я перепишу секцію "Огляд", дотримуючись усіх ваших обмежень: лаконічний, поведінковий опис (ЩО і НАВІЩО), чистий Markdown, українська мова, без сигнатур/типів/параметрів/вступу/висновків, без обгортки, та без згадок про кешування чи Read-only.

Будь ласка, надайте мені файл, який містить цю чернетку, або скажіть, який саме файл потрібно редагувати, щоб я міг виконати завдання відповідно до конвенцій проєкту.

Якщо я маю просто виконати переписаний текст, базуючись на ваших інструкціях та чернетці:

Ось переписаний варіант тексту, згідно з вашими вимогами (припускаючи, що це конфігураційний файл, який налаштовує середовище для тестів):

Файл налаштовує середовище виконання тестів, інтегруючи конфігурації Vitest та Vite. Він сфокусований на тестуванні файлів з розширеннями `.test.js` або `.test.mjs` і забезпечує симуляцію браузерного середовища за допомогою `happy-dom`, вимагаючи явного імпорту всіх залежностей. Крім того, він примусово вбудовує вихідний код `@7n/tauri-components` для коректної компіляції `.vue` файлів та організовує збір покриття коду за допомогою `lcov` і `text-summary`.

## Поведінка

1. Конфігурує Vitest для виконання тестів, інтегруючи налаштування від Vite для режиму тестування.
2. Визначає, що тестування включає файли з розширеннями `.test.js` або `.test.mjs` у директоріях `src` та `tests`.
3. Встановлює середовище тестування `happy-dom`, що забезпечує симуляцію браузерного середовища.
4. Встановлює режим роботи без глобальних об'єктів, що вимагає явного імпорту всіх залежностей.
5. Примусово вбудовує вихідний код з пакета `@7n/tauri-components`, щоб плагін Vue міг скомпілювати `.vue` файли, а не намагатися імпортувати їх як чисті файли Node.
6. Налаштовує збір покриття коду, використовуючи провайдера `v8` та репортери `lcov` і `text-summary`.

## Гарантії поведінки

- Read-only: не виконує операцій запису (ФС/БД).
