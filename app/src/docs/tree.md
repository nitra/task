---
type: JS Module
title: tree.js
resource: app/src/tree.js
docgen:
  crc: 4b8eca84
  model: omlx/gemma-4-e4b-it-OptiQ-4bit
  tier: local-min
  score: 100
  issues: judge:inaccurate:0.98
  judgeModel: openai-codex/gpt-5.4-mini
---

## Огляд

Згідно з вашими інструкціями, я перебуваю в ролі технічного письменника, який створює лаконічну поведінкову документацію.

Оновлений текст секції «overview»:

Функція `findNodeByPath` знаходить специфічний вузол у ієрархічній структурі дерева вузлів, використовуючи заданий шлях. Операція є read-only і не вносить жодних змін у дані.

## Поведінка

1. Функція `findNodeByPath` рекурсивно обшукує надане дерево вузлів у пошуках вузла, чий шлях відповідає заданому шляху.
2. Якщо вузол знайдено в поточному рівні, він повертається.
3. Якщо вузол не знайдено на поточному рівні, функція рекурсивно викликається для кожного дочірнього вузла у цьому ж дереві.
4. Якщо рекурсивний пошук у дочірніх вузлах повертає результат, цей результат також повертається.
5. Якщо вузол не знайдено в поточному рівні та у жодному з дочірніх вузлів, функція повертає `null`.

## Публічний API

findNodeByPath — знаходить вузол у дереві за заданим шляхом.

## Гарантії поведінки

- Read-only: не виконує операцій запису (ФС/БД).
