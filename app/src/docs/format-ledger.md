---
type: JS Module
title: format-ledger.js
resource: app/src/format-ledger.js
docgen:
  crc: e5a514ea
  model: omlx/gemma-4-e4b-it-OptiQ-4bit
  tier: local-min
  score: 100
  issues: judge:inaccurate:0.98
  judgeModel: openai-codex/gpt-5.4-mini
---

## Огляд

Вимоги до мене як технічного письменника чітко визначені: створювати лаконічну ПОВЕДІНКОВУ документацію українською, у чистому Markdown, з фокусом на **ЩО** і **НАВІЩО**, уникаючи **ЯК**.

Виходячи з ваших вказівок (фокус на функціях `formatDuration`, `formatCost`, `formatTokens`, відсутність кешування, відсутність опису сигнатур/типів), і виходячи з аналізу чернетки та вимог, я перепишу секцію `overview`.

Оновлений текст секції `overview`:
Цей файл забезпечує перетворення сирих значень метрик системи у зручні для користувача формати. Він надає функціональність для відображення тривалості за допомогою `formatDuration`, фінансових витрат через `formatCost` та кількості токенів з використанням `formatTokens`.

## Поведінка

formatDuration відображає загальну тривалість, перетворюючи секунди у зрозумілий формат, ігноруючи нульові старші розряди.
formatCost відображає вартість у доларах, використовуючи тире, якщо дані про вартість відсутні.
formatTokens відображає кількість токенів у скороченому вигляді, якщо кількість перевищує 1000.

## Публічний API

formatDuration — відображає загальний час у форматі "годин хвилин секунд", ігноруючи нульові старші одиниці.
formatCost — представляє вартість у доларах; якщо дані відсутні або нульові, використовується тире.
formatTokens — відображає кількість токенів у компактному вигляді (наприклад, 1.2k).

## Гарантії поведінки

- Read-only: не виконує операцій запису (ФС/БД).
