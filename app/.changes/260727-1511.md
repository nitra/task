---
bump: minor
section: Changed
---
Реліз-пайплайн: build-тригер і changelog-якір об'єднано в один тег `app@X.Y.Z` (замість окремого `vX.Y.Z`); GitHub-реліз тепер рахує "What's Changed" явно від попереднього тега тієї самої продуктової сім'ї, а не від автовизначення GitHub (яке "залипало" на дуже старій базі). Updater переїхав на канал `app-latest` (`task-latest` лишається постійним legacy-аліасом для вже встановлених білдів).
