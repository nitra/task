---
bump: patch
section: Fixed
---

Реліз v0.5.3 падав на codesign: Tauri підписує додаткові бінарники лише зі скану `src/bin/` — journal повернуто у `src/bin/journal.rs` (перевірена топологія v0.4.1), а doc-files-дзеркало `src/bin/docs/` додано у `.gitignore`, щоб воно більше ніколи не потрапляло у CI-checkout і не ламало бандлер (корінь фейлів v0.5.0–v0.5.2).
